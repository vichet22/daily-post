{
  "name": "daily-post",
  "version": "1.0.0",
  "description": "Daily Post Flask Application",
  "scripts": {
    "start": "cd backend && python app.py",
    "dev": "cd backend && python app.py"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
